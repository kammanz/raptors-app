/**
 * Enum for common colors.
 * @readonly
 * @enum {string}
 */
export const COLOR = Object.freeze({
    GREY: '#F5F5F5',
});

/**
 * Enum for default team, set to raptors.
 * @readonly
 * @enum {string}
 */
export const DEFAULT_TEAM = Object.freeze({
    TRI_CODE: 'TOR',
    TEAM_ID: 1610612761,
});
